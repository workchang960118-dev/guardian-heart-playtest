# UI refresh v10

本輪依使用者新口徑，重點收斂為：

1. 把「流程控制」拉到最頂端，取代原本過大的戰局摘要塊。
2. 把「目前事件」左置並縮成精簡事件摘要，不再佔用過大橫幅高度。
3. 繼續壓縮資訊密度與字體層級，讓單屏閱讀更接近目標。
4. 再次縮窄左右欄、把中間地圖區比例拉高。
5. 任務列維持 5 張寬度基準，超出以左右滑動處理。

## 主要調整
- Sticky header 重組為：流程控制 / 目前事件 / 核心指標與次要按鈕。
- 移除原本右側大塊流程控制卡，避免與主舞台重複。
- 主舞台 section 與任務列 padding 再縮小。
- 左側玩家卡、右側手牌卡、任務卡字級與 padding 再壓縮。
- 主舞台欄位從 `172 / 2.18fr / 198` 收斂為 `160 / 2.3fr / 188`。

## 本輪目標
更接近：
- 一個視窗內看完主要資訊
- 中央地圖是唯一主角
- 一般玩家模式不被 debug / observer / 開發資訊干擾
